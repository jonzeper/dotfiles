alias reload!='. ~/.zshrc'
alias ls='ls -al'
alias apache2ctl='sudo /opt/local/apache2/bin/apachectl'

# Allow me to do stuff like rake task[param] without complaining about brackets
alias rake='noglob rake'

alias d-c='docker-compose'
